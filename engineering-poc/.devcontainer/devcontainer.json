{
  "name": "Engineering POC",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "22" }
  },
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && python -m pip install -e './backend[test]' && cd frontend && npm install && npx playwright install --with-deps chromium",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-playwright.playwright"],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.testing.pytestEnabled": true
      }
    }
  }
}
